{
  "name": "sixt-deal-scanner",
  "version": "0.3.0",
  "description": "Apify Actor: SIXT corporate rate scanner. In-cloud manual login via Live View (save-session) + headless scan. No password, no CAPTCHA/2FA bypass, no auto-booking.",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node main.js",
    "capture-session": "node capture-session.js"
  },
  "dependencies": {
    "apify": "^3.7.2",
    "playwright": "^1.60.0"
  }
}
